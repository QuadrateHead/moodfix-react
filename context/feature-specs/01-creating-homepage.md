Read `AGENTS.md` before starting

We are adding the design system and UI of web site following screenshots. Pages should be in /src/pages, components in /src/components and elements in /src/elements

HomePage:

- Create a HomePage following currently open figma file.
- Paste a pacifier(No-Poster frame from Figma) where should be part for functionality(for example array.map blocks). Use images, text and etc. from Figma file like temporary content
  MoviePage:
- Create a MoviePage following currently open figma file.
- Paste a text from Figma file instead of parts where should be part for functionality(for example movie.title and else)
  Do not include Functionality and API
  Use sources from Figma file (svg,images,sizes,borders and etc.)

### Check then done

- All components import without errors
- All paths are correct
- Completed UI look exactlty like .fig file frames
- No default light styling appears
- index.css didn't changed
